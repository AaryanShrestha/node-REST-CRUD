const express = require ('express');

const router = express();

router.post('/postproduct/:userid', );
router.get('/getallproduct/:userid', );
router.get('/getoneproduct/:userid', );
router.update('/updateproduct/:userid', );
router.delete('/deleteproduct/:userid', );

module.exports = router;