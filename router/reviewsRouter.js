const express = require ('express');

const router = express();

router.post('/postreview/:userid', );
router.get('/getallreview/:userid', );
router.get('/getonereview/:userid', );
router.update('/updatereview/:userid', );
router.delete('/deleterouter/:userid', );

module.exports = router;
