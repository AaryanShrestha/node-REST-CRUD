const express = require ('express');

const router = express();

router.post('/postorder/:userid', );
router.get('/getoneorder/:userid', );
router.get('/getallorder/:userid', );
router.update('/updateorder/:userid', );
router.delete('/deleteorder/:userid' );

module.exports = router;